2026-05-01 — Issue #2 (T1: Scaffold projet & toolchain)

Scaffolded the project from scratch per PROJECT.md §12. All quality gates green:
  - bun run dev → Vite up, serves "Reverso"
  - bun run build → production bundle + PWA SW
  - bun run typecheck → tsc --noEmit clean
  - bun run lint → oxlint clean
  - bun run format:check → oxfmt clean
  - bun run test → smoke test (App renders heading) passes

Tooling decisions worth flagging for the next agent:
  - TypeScript: `bun add -d typescript@beta` resolved to 6.0.3 (the current @beta
    tag). PROJECT.md §3.11 names "TS 7 beta" as the target; 6.0.3 is the closest
    available now and works without compat shims, so no fallback to 5.x was
    needed. Re-check `npm dist-tag ls typescript` periodically.
  - Bun lockfile: bun 1.3.13 writes `bun.lock` (text) by default, not the older
    `bun.lockb` (binary) named in PROJECT.md §9.3 / §12. I committed `bun.lock`.
    No action needed; just a naming drift in PROJECT.md.
  - tsconfig: removed `baseUrl` (deprecated in TS 6/7) and use a `paths` map
    relative to the config dir for the `@/*` alias. Vite alias mirrors it via
    `fileURLToPath`.
  - vitest config: split into a separate `vitest.config.ts` that mergeConfig's
    the vite config — `vite.config.ts` types in vite 8 don't accept a `test`
    field anymore.
  - PWA icons in `public/` are 1x1 transparent PNG placeholders. Real icons are
    a T7 deliverable (issue #8).
  - Vitest 4 dropped the `--related` flag in favor of a `vitest related <files>`
    subcommand. The pre-commit hook uses the new form. PROJECT.md §9.2 still
    names `vitest run --related` — update that note when convenient.

Pre-commit hook verified by attempting a commit with a TS error: lint-staged
ran oxlint+oxfmt --check on staged files, then `tsc --noEmit` blocked the
commit with the expected TS2322 error. No `--no-verify` used.

Directory layout matches PROJECT.md §6 (audio/, audio/wrappers/, components/,
phases/, store/ — currently with `.gitkeep` placeholders, ready for T2+).

Next up: issue #3 (T2 — démo audio: enregistrement + lecture forward).

2026-05-01 — Issue #3 (T2: Démo audio — enregistrement + lecture forward)

Built the audio pipeline scaffold and a working demo page. After this slice the
app records mic input, decodes the WebM/Opus blob to an AudioBuffer, and plays
it back forward at 1×. All gates green (typecheck, 29 tests across 6 files,
oxlint, oxfmt --check, vite build incl. PWA SW).

Modules delivered:
  - src/audio/decodeRecording.ts (+ .test.ts) — pure async fn,
    Blob → ArrayBuffer → AudioBuffer via injected AudioContext.decodeAudioData.
    5 tests, all paths covered (mono/stereo, decode failure, blob.arrayBuffer
    failure short-circuits before decodeAudioData).
  - src/audio/wrappers/recorder.ts (+ .test.ts) — factory
    createRecorder({ maxDurationMs }) → { start, stop, cancel }. Wraps
    getUserMedia + MediaRecorder, enforces maxDurationMs via setTimeout that
    triggers requestStop, stops all stream tracks on finalize. 10 tests with
    fake MediaRecorder + getUserMedia stubs.
  - src/audio/wrappers/player.ts (+ .test.ts) — factory
    createPlayer(ctx) → { load, play, pause, onEnded }. Recreates an
    AudioBufferSourceNode per play() (one-shot lifecycle), disposes on
    pause/replay, only fires onEnded when the source ended naturally (not on
    manual pause). Resumes the AudioContext if suspended. 9 tests.
  - src/components/AudioRecorder.tsx (+ .test.tsx) — UI: Enregistrer / Arrêter
    buttons, Décodage state, error + Réessayer on permission denial. Uses
    injected recorderFactory / audioContextFactory / decode for testability.
  - src/components/AudioPlayer.tsx (+ .test.tsx) — minimal play/pause toggle,
    auto-resets to "Lecture" label when the player reports end-of-buffer.

Design notes worth flagging for the next agent:

  - **Wrapper injection convention**: components accept `recorderFactory` /
    `playerFactory` / `audioContextFactory` props with browser-API defaults.
    Tests pass fakes; production wires up real APIs. Phase components in T4+
    should follow the same convention so we never need a real AudioContext in
    jsdom.
  - **AudioRecorder emits AudioBuffer (not Recording)**: per T2 acceptance
    criteria. T3 (issue #4) will (a) add reverseBuffer, (b) change the
    AudioRecorder callback to emit a `Recording = { forward, reverse,
    durationMs }`, (c) extend the player wrapper with setRate/setDirection,
    and (d) add a slider + direction toggle to AudioPlayer. Plan to keep the
    AudioRecorder API change in one commit to avoid two passes through the
    component tests.
  - **AudioContext lifecycle in App**: App.tsx lazily creates a single
    AudioContext via a ref (audioContextRef) and shares it between the
    recorder's decode step and the player. This matters because most browsers
    cap the number of contexts. T4 should likely move this into a small
    `useAudioContext` hook or the Zustand store once more components need it.
  - **MediaRecorder mimeType**: we read `mediaRecorder.mimeType` and fall back
    to "audio/webm" if empty (jsdom returns empty). Real browsers will return
    a usable type; iOS Safari returns "audio/mp4". decodeAudioData handles
    both, but note this if we ever want to inspect the container.
  - **Recorder state machine**: a Session has an `active` flag; once
    auto-stop or manual stop has finalized, `active` flips false but the
    Session lingers so a late `stop()` call can still await the cached
    finalize promise. start() can replace a finalized session. cancel() nulls
    the session and suppresses the finalize resolution. The "active" flag is
    the one place to look if behavior here gets weird.
  - **No coverage tool wired into the gate** (yet). The 100% claim for
    decodeRecording/reverseBuffer per PROJECT.md §8 is by inspection, not
    enforced. T3 might be a good moment to add `bun run coverage` if we want
    a CI-like signal locally.
  - **@testing-library/user-event added** as a devDep — needed for click /
    type interactions in RTL tests. Standard companion to RTL, no surprise.
  - **`.gitkeep` placeholders removed** from src/audio, src/audio/wrappers,
    src/components now that those dirs have real files. src/phases and
    src/store still hold .gitkeep — they'll be populated in T4.

Next up: issue #4 (T3 — inversion + slider vitesse + toggle direction).

2026-05-01 — Issue #4 (T3: Inversion + slider vitesse + toggle direction)

Audio pipeline now feature-complete for the gameplay loop. Demo page records →
plays forward → toggles reverse → varies speed (0.25–2× with snap on 1×). All
gates green: 49 tests across 8 files, typecheck clean, oxlint clean,
oxfmt --check clean, vite build OK (PWA SW generated).

Modules delivered:
  - src/audio/recording.ts — shared `Recording = { forward, reverse, durationMs }`
    + `Direction` type. Standalone module so components, wrappers and (future)
    store can all import it without a circular dep.
  - src/audio/reverseBuffer.ts (+ .test.ts) — pure fn `(AudioBuffer, factory)
    => AudioBuffer`. The factory is either a `(channels, length, sampleRate)
    => AudioBuffer` callback (used in tests) or an `AudioContext` whose
    `createBuffer` is invoked. 7 tests: mono/stereo/odd-length/single-sample,
    sample-rate + channel count + length preserved, source not mutated,
    AudioContext path. Per-channel reverse via index walk on a fresh
    Float32Array allocated by the factory — source channel data is read but
    never mutated.
  - src/audio/wrappers/player.ts — extended:
      • `load(Recording)` (was `load(AudioBuffer)`)
      • `setRate(rate)` — applied to live source's `playbackRate.value` if
        playing; otherwise stored for the next play().
      • `setDirection('forward'|'reverse')` — if not playing, just stores it;
        if playing AND the new direction differs, disposes the current source
        and re-starts a new one with the other buffer (rate preserved). The
        same-direction case is a no-op.
    17 tests now (was 9) — covered same-direction no-op, paused-direction-set
    no-op, rate preserved across direction swap, no spurious onEnded fired
    when setDirection replaces the source mid-playback.
  - src/components/AudioRecorder.tsx — callback signature changed: emits a
    `Recording`. Computes both buffers via injected `decode` then `reverse`,
    derives `durationMs` as `forward.length / forward.sampleRate * 1000`.
    Test updated; new `reverse` prop wired with the same factory-injection
    convention as `decode`/`recorderFactory`.
  - src/components/AudioPlayer.tsx — props changed: takes a `recording` (was
    `buffer`) plus an optional `initialDirection`. UI now renders:
      • play/pause toggle (existing)
      • a "Sens : à l'endroit / à l'envers" toggle button
      • a `<input type="range">` 0.25–2.0 step 0.01 with `aria-label="Vitesse"`,
        snapping to 1.0 within ±0.05 (constant SNAP_RANGE in the file).
    7 tests (was 2): slider change → setRate, slider snap on 1×, extreme
    values 0.25 / 2.0, direction toggle alternates calls, initialDirection
    primes the player on mount.

Design notes worth flagging for the next agent:

  - **Why factory injection for reverseBuffer**: the function needs to allocate
    a fresh AudioBuffer. In jsdom there is no AudioContext, so the test passes
    a `(channels, length, sampleRate) => AudioBuffer` factory directly.
    Production code passes an AudioContext (the function detects it by
    `typeof factory === "function"` vs object) and calls `ctx.createBuffer`.
    Keeps the function pure-ish without coupling to the AudioContext API.
  - **Direction swap = source recreate, no offset preservation**: when
    setDirection is called mid-playback, we dispose and start a new source
    from t=0 with the other buffer. Restoring playback offset via
    `start(when, offset)` was tempting but adds complexity (AudioContext
    timeline math) and is not in PROJECT.md §3.6's spec ("swap entre les
    deux buffers"). If users complain about the audible restart, the place
    to add offset preservation is `setDirection` in player.ts — read
    `audioContext.currentTime - playStartTime` and pass to `start()`.
  - **initialDirection prop on AudioPlayer**: added now (not strictly required
    by T3 AC, but trivial and unblocks T5's GuessingPhase, which needs the
    "original" player to start in `reverse`). One ref-check on first render
    calls setDirection if non-default. Doesn't react to prop changes after
    mount — that would require a useEffect; punt until T5 actually needs it.
  - **AudioPlayer slider testing**: native `dispatchEvent` does NOT trigger
    React's synthetic onChange in jsdom because the React 19 controlled-input
    intercept reads via property descriptors that the bare `value =` assign
    bypasses. Used `fireEvent.change(input, { target: { value } })` from
    @testing-library/react instead — that one works.
  - **Recording vs AudioBuffer in the player**: only the player owns the
    "which side am I playing" state. Components above (AudioPlayer,
    AudioRecorder) don't need to know the two buffers exist beyond storing
    them. Keep this boundary as we wire the store in T4 — it should hold
    Recording, not two separate AudioBuffer fields.
  - **No coverage tool wired into the gate** (still). reverseBuffer and
    decodeRecording are 100% by inspection. If we want enforcement, T4 would
    be the moment (the gameStore tests will be the next batch claiming 100%).
  - **MIN_RATE / MAX_RATE / SNAP_RANGE** are file-local constants in
    AudioPlayer.tsx. If T4+ wants to make the range configurable per phase,
    promote them to props. For now the same constants apply to A's preview,
    B's player, and the reveal players — matches PROJECT.md §3.3.

Next up: issue #5 (T4 — state machine + phases Menu/Permission/HandoffA/RecordingA).

2026-05-01 — Issue #5 (T4: State machine + phases Menu/Permission/HandoffA/RecordingA)

Wired the first half of a real round on top of the existing audio primitives.
App now boots into a Zustand-driven phase machine and walks Menu → Permission
→ HandoffA → RecordingA → Done (placeholder). All gates green: 76 tests
across 14 files, typecheck clean, oxlint clean, oxfmt --check clean, vite
build OK (PWA SW generated).

Modules delivered:
  - src/store/gameStore.ts (+ .test.ts) — single Zustand store. State shape
    `{ phase, originalRecording, guessRecording, notes }`. Phase type is
    `'menu' | 'permission' | 'permissionDenied' | 'handoffA' | 'recordingA'
    | 'done'` (the last one is a placeholder until T5 lands handoffB+). Seven
    actions: startGame, permissionGranted, permissionDenied, retryPermission,
    confirmHandoffA, finishRecordingA, backToMenu. Invalid actions in the
    wrong phase are no-ops (decision documented in this note: the phase
    component never gets to dispatch the wrong action in normal flow, and
    no-op is less disruptive than throwing for misclick edge cases). 17 tests
    covering each transition + each invalid-state no-op + two end-to-end
    walks (happy path and permission-denied retry).
  - src/phases/MenuPhase.tsx (+ .test.tsx) — heading + "Démarrer une partie"
    button → startGame.
  - src/phases/PermissionPhase.tsx (+ .test.tsx) — useEffect calls an injected
    `requestPermission` (default: `navigator.mediaDevices.getUserMedia`).
    Resolves → stops the granted stream's tracks (we only wanted the prompt;
    AudioRecorder will request its own stream when it actually records) →
    dispatches permissionGranted. Rejects → permissionDenied. Cancellation
    flag in the effect cleanup avoids dispatch-after-unmount on hot
    transitions. 2 tests cover both branches.
  - src/phases/PermissionDeniedPhase.tsx (+ .test.tsx) — alert + collapsible
    iOS/Android instructions + "Réessayer" button (retryPermission) +
    "Retour au menu" button (backToMenu). 2 tests.
  - src/phases/HandoffAPhase.tsx (+ .test.tsx) — message + "Continuer"
    button → confirmHandoffA. 1 test.
  - src/phases/RecordingAPhase.tsx (+ .test.tsx) — wraps the existing
    AudioRecorder with maxDurationMs=15000, forwarding finishRecordingA as
    onRecorded. Accepts the same injection props as AudioRecorder
    (audioContextFactory, recorderFactory, decode, reverse) so jsdom tests
    can stub everything. 1 golden-path test that records → stops → asserts
    store transitioned to `done` with `originalRecording` populated.
  - src/phases/DonePhase.tsx (+ .test.tsx) — temporary terminal screen with
    a "Retour au menu" button. Exists only to give T4 a closed loop; T5
    replaces it with HandoffB. 1 test.
  - src/App.tsx — replaces the demo page. Reads `phase` from the store and
    renders the matching phase component. Owns the lazy AudioContext via a
    ref (same pattern as before) and passes `getAudioContext` to
    RecordingAPhase. App.test.tsx updated: 3 tests covering menu render,
    handoffA render via setState, permissionDenied render via setState.

Design notes worth flagging for the next agent:

  - **Invalid-state actions are no-ops, not throws**. Implementation is
    `set((s) => s.phase === EXPECTED ? { ... } : {})`. Reasoning: the UI
    only exposes an action's button while in the corresponding phase, so
    in practice the wrong-phase path is dead. Choosing no-op over throw
    means a hot-reload, double-click, or stale-closure dispatch can't crash
    the app. If T5+ ever needs to *detect* an invalid dispatch (e.g. for
    logging), revisit — the cheapest path is to switch to throws and let
    the call sites catch.
  - **Test reset uses merge, not replace**. `useGameStore.setState({
    ...INITIAL_STATE })` (no `true` second arg). The replace mode wipes the
    action functions off the store, breaking subsequent action calls in
    the same test. This is documented in zustand but is a foot-gun on
    first encounter. INITIAL_STATE is exported for this exact use.
  - **PermissionPhase calls getUserMedia and immediately stops the tracks**.
    Why: the only goal of this phase is to surface the browser permission
    prompt. The actual recording stream is requested again by the recorder
    wrapper inside RecordingAPhase. After permission is granted, the
    second getUserMedia call resolves silently with no prompt. This avoids
    keeping a hot mic open across the HandoffA screen. Confirmed in iOS
    Safari and Chrome behavior docs; not e2e-tested (anti-goal).
  - **AudioContext is owned by App, passed as a factory**. Same pattern as
    pre-T4. RecordingAPhase takes `audioContextFactory: () => AudioContext`
    and forwards it to AudioRecorder. If T5 needs the same context in
    GuessingPhase (likely — both AudioRecorder and AudioPlayer want it),
    the right move is to extract a `useAudioContext` hook or stash it in
    the store. Don't create two contexts — most browsers cap them.
  - **Phase components dispatch via the store, not props**. They call
    `useGameStore((s) => s.action)` directly. This makes them harder to
    unit-test in isolation, but the store is itself well-tested and easy
    to seed via setState — net positive ergonomics. The same convention
    should hold for T5's HandoffB / Guessing / ConfirmEnd phases.
  - **DonePhase is throw-away**. It exists only so the T4 loop closes and
    a user doesn't get stuck. T5 (issue #6) replaces it with HandoffBPhase
    and the `done` value drops out of the Phase union. Don't add features
    here.
  - **Zustand v5 added** (`bun add zustand` → 5.0.12). PROJECT.md §4 lists
    zustand without a version; no surprise.
  - **Pre-commit gate verification skipped** in this session — I ran the
    individual scripts (typecheck/test/lint/format:check/build) but not
    `husky` itself. The hook should still fire on commit; if it fails
    because of staged-only oxfmt issues, run `bun run format` and re-stage.

Next up: issue #6 (T5 — HandoffB + Guessing + ConfirmEnd).

2026-05-01 — Issue #6 (T5: HandoffB + Guessing + ConfirmEnd)

Closed the second half of a round. After this slice the app walks Menu →
Permission → HandoffA → RecordingA → HandoffB → Guessing → ConfirmEnd →
Reveal (placeholder), and a cancel from ConfirmEnd returns to Guessing
without losing notes / guess recording / listen counter. All gates green:
102 tests across 18 files, typecheck clean, oxlint clean, oxfmt --check
clean, vite build OK (PWA SW generated).

Modules delivered:
  - src/store/gameStore.ts — Phase union extended:
    `'menu' | 'permission' | 'permissionDenied' | 'handoffA' | 'recordingA'
    | 'handoffB' | 'guessing' | 'confirmEnd' | 'reveal'`. The placeholder
    `done` from T4 is gone. `finishRecordingA` now transitions to
    `handoffB` (was `done`). New state slice `listenCount: number` (in
    INITIAL_STATE → 0). New actions:
      • confirmHandoffB (handoffB → guessing)
      • setNotes(notes) — guarded to guessing phase only
      • setGuessRecording(rec) — guarded to guessing phase only;
        overwrites previous (one-prise convention from PROJECT.md §3.3)
      • incrementListenCount — unguarded, just bumps the counter
      • endGuessing (guessing → confirmEnd)
      • cancelEnd (confirmEnd → guessing) — preserves notes / guess /
        counter naturally because we only set `phase`
      • confirmEnd (confirmEnd → reveal)
    backToMenu still resets to INITIAL_STATE so listenCount goes back to
    0 too. 23 store tests now (was 17), covering every transition + every
    invalid-state no-op + two end-to-end walks (full happy path through
    reveal, and cancelEnd round-trip preserving slices).
  - src/components/NotesEditor.tsx (+ .test.tsx) — minimal controlled
    multi-line textarea. `value` + `onChange` props, that's it. Labelled
    "Notes" via wrapping label so the textarea is found by RTL via
    `getByRole('textbox', { name: /notes/i })`. 2 tests.
  - src/components/AudioPlayer.tsx — added optional `onPlay?: () => void`
    callback fired ONLY when transitioning idle → playing (not on the
    pause click, not on resume from end-of-buffer). GuessingPhase wires
    this up to incrementListenCount on the original player. 1 new test
    asserts onPlay fires exactly once per play click and not on pause.
  - src/phases/HandoffBPhase.tsx (+ .test.tsx) — message + "Continuer"
    button → confirmHandoffB. Symmetric to HandoffAPhase. 1 test.
  - src/phases/GuessingPhase.tsx (+ .test.tsx) — composes everything:
      • AudioPlayer for `originalRecording` with `initialDirection="reverse"`
        and `onPlay={incrementListenCount}`
      • A `<p aria-label="Compteur d'écoutes">Écoutes : {listenCount}</p>`
        next to the original player (informative, non-blocking — stored
        in the gameStore so it survives the cancelEnd round-trip)
      • NotesEditor bound to gameStore.notes via setNotes
      • AudioRecorder (max 15s) for B's voice, onRecorded → setGuessRecording
        (overwrites silently per PROJECT.md §3.3)
      • A second AudioPlayer for `guessRecording` rendered conditionally
        only after the first take exists
      • "Fin" button → endGuessing
    Defensive: if `originalRecording` is null (entered the phase via a
    direct setState, not the normal flow), renders an alert and no players.
    Accepts the same injection props as RecordingAPhase
    (audioContextFactory, recorderFactory, decode, reverse) plus a
    playerFactory passed through to AudioPlayer for jsdom tests. 5 tests:
    primes player to reverse, "Fin" → confirmEnd, NotesEditor wired,
    listen counter increments and updates UI, recording → store
    population + second player appears.
  - src/phases/ConfirmEndPhase.tsx (+ .test.tsx) — `<section role="dialog"
    aria-modal="true">` with "Confirmer" → confirmEnd action and
    "Annuler" → cancelEnd action. 2 tests covering both buttons; the
    cancel test asserts notes / guessRecording / listenCount survive.
  - src/phases/RevealPhase.tsx (+ .test.tsx) — placeholder. Heading,
    explanation, "Retour au menu" button (backToMenu). T6 (issue #7)
    replaces this with the real reveal screen. 1 test.
  - src/phases/DonePhase.tsx (+ .test.tsx) — REMOVED. Was a T4
    placeholder; now gone, replaced by `RevealPhase` placeholder + new
    real terminal phases.
  - src/App.tsx — phase switch updated: removed `done` branch, added
    `handoffB`, `guessing`, `confirmEnd`, `reveal`. GuessingPhase shares
    the same lazy AudioContext (via getAudioContext) as RecordingAPhase.

Design notes worth flagging for the next agent:

  - **listenCount lives in the store, not in GuessingPhase local state**.
    Reason: cancelEnd transitions confirmEnd → guessing, which unmounts
    the GuessingPhase component (App swaps phase components — only one
    renders at a time). Local state would reset on remount; the AC
    explicitly requires the counter to survive cancelEnd. Putting it in
    the store also means backToMenu (which already wipes to
    INITIAL_STATE) clears it for free. The AudioPlayer's local rate /
    direction state still resets on remount — that's not in the AC, so
    we leave it.
  - **AudioPlayer.onPlay fires only on idle → playing**. Not on pause,
    not on the synthetic "ended" event. This is what we want for a
    listen counter ("how many distinct listens did B do?"); a different
    counting policy (e.g. count every press) would require revisiting.
    The hook is deliberately optional — pre-T5 callers of AudioPlayer
    don't need to change.
  - **GuessingPhase null-guard on originalRecording**. The phase should
    only ever be reached after recordingA, so originalRecording is
    guaranteed non-null in real flow. The guard exists for defensive
    rendering when tests / hot-reloads jump straight into the phase via
    setState. Same pattern is appropriate for RevealPhase in T6 (it
    needs both originalRecording and guessRecording).
  - **Two AudioPlayer instances share one AudioContext**. The
    GuessingPhase calls audioContextFactory() once at the top and passes
    the same ctx to both players. App.tsx's getAudioContext is memoized
    via a ref so subsequent calls return the same instance — important
    because most browsers cap AudioContexts. If a future phase needs a
    different ctx per player, that's a new design decision.
  - **Action naming follows PROJECT.md §5.1**. We use `endGuessing` for
    the request-to-end (guessing → confirmEnd) and reserve `confirmEnd`
    for the actual confirmation action (confirmEnd → reveal), matching
    the spec. `cancelEnd` is also from §5.1.
  - **Phases that take audioContextFactory now: RecordingAPhase,
    GuessingPhase**. RevealPhase will also need it in T6 (two players).
    Keep the factory pattern — don't move to a hook or a context yet,
    it's overkill for two callers and one prop.
  - **Notes textarea uses a plain controlled input**. No debounce, no
    persistence — typing every character calls the store action. The
    store's setNotes is a single set() so this is cheap, and the
    no-persistence anti-goal (PROJECT.md §2) means we don't need to
    worry about write-amplification.
  - **No coverage tool wired into the gate** (still). The store is at
    100% by inspection (every action has a happy-path + invalid-state
    test). reverseBuffer / decodeRecording also still 100% by
    inspection.

Next up: issue #7 (T6 — Phase Reveal + flow Nouvelle partie / Retour menu).

2026-05-01 — Issue #7 (T6: Phase Reveal + flow Nouvelle partie / Retour menu)

Closed the round loop. After this slice an entire round Menu → Reveal is jouable,
two rounds enchaînés via "Nouvelle partie" (back into handoffA, fresh slices),
and "Retour menu" hard-resets the store. All gates green: 108 tests across 18
files, typecheck clean, oxlint clean, oxfmt --check clean, vite build OK
(PWA SW generated, 6 precache entries / 198.85 KiB).

Modules delivered:
  - src/store/gameStore.ts — new `newRound` action, guarded to the `reveal`
    phase. Resets `originalRecording`, `guessRecording`, `notes`,
    `listenCount` and transitions to `handoffA` (skips the menu/permission
    prelude on purpose — permission was already granted earlier in the
    session). Added to ActionKey/Slice typing. The PRD AC explicitly named
    only the three slice fields; I also reset `listenCount` because it's
    per-round and would otherwise carry over visibly into Mode 1's Mode-3-
    ready counter UI in T?+. 25 store tests now (was 23): newRound happy
    path + invalid-state no-op.
  - src/phases/RevealPhase.tsx — replaces the T5 placeholder. Renders:
      • `<AudioPlayer>` for `originalRecording` (default `forward`, full
        slider + direction toggle inherited from the component)
      • `<AudioPlayer>` for `guessRecording`, only when non-null (B may have
        ended the round without recording — null-safe)
      • `<p aria-label="Notes">{notes}</p>` when notes are non-empty;
        "Aucune note prise." italic placeholder otherwise
      • "Nouvelle partie" → newRound, "Retour au menu" → backToMenu
    Defensive null-guard on `originalRecording` mirrors GuessingPhase: if
    the phase is entered via setState with no recording, render a single
    alert + back-to-menu button (no players).
    Accepts the same `audioContextFactory` + optional `playerFactory` as
    GuessingPhase; both players share one ctx via the App-level lazy ref.
  - src/phases/RevealPhase.test.tsx — rewritten. 5 tests: two players +
    notes shown when both recordings present; only one player + "Aucune
    note prise" placeholder when guessRecording is null; Nouvelle partie
    → handoffA + slices reset; Retour menu → menu + full reset; defensive
    alert when originalRecording is null.
  - src/App.tsx — passes `getAudioContext` to RevealPhase. Same shared lazy
    AudioContext used by RecordingAPhase / GuessingPhase / RevealPhase.

Design notes worth flagging for the next agent:

  - **`newRound` lands in `handoffA`, not `permission`**. Reasoning: micro
    permission was granted earlier in this app session (at the very first
    round). Re-prompting on every round is annoying and unnecessary;
    `getUserMedia` will resolve silently the second time. If a user
    manually revoked the perm, the next `getUserMedia` inside RecordingA
    will throw and we'll surface it via the existing recorder-error path.
    If T?+ ever wants to re-validate permission per round, jump to
    `permission` here instead — the state machine already handles it.
  - **`backToMenu` from RevealPhase is the same hard reset** used from
    PermissionDeniedPhase / DonePhase-of-T4. INITIAL_STATE wipe via the
    existing action — no new code needed in the store. The defensive
    "no original recording" branch also exposes a back-to-menu button so
    a stuck user always has an exit.
  - **Two AudioPlayer instances share one AudioContext**, same as
    GuessingPhase. App.tsx's getAudioContext is memoized via a ref; the
    RevealPhase calls it once at the top and passes the same ctx to both
    players. Don't create two contexts here — most browsers cap them.
  - **Notes are rendered as a single `<p>`**, not a `<pre>` or rich text.
    NotesEditor accepts multi-line input via textarea, but on reveal we
    don't preserve newlines visually. If users complain, switch to
    `white-space: pre-wrap` styling or a `<pre>` element here. Not in T6
    AC and not worth the polish budget yet.
  - **Direction toggle on the original player defaults to `forward`** in
    RevealPhase (was `reverse` in GuessingPhase). Player B has just heard
    the reversed version exhaustively; the moment of revelation is
    hearing the forward original. The toggle button is still present so
    they can flip back to compare with what they were guessing against.
  - **Coverage tool still not wired in**. `gameStore` is at 100% by
    inspection (newRound has happy + invalid-state tests like every
    other action). reverseBuffer / decodeRecording also still 100% by
    inspection. T7 polish slice would be the moment to wire this — or
    leave it permanently unenforced since pre-commit runs the full
    `vitest run` anyway, not `--related`.
  - **RevealPhase is the last functional slice for Mode 1**. T7 (issue #8)
    is PWA polish + HITL device test only — no new state machine work,
    no new components beyond manifest/icon assets. The only Phase union
    left untouched in the lifecycle is now exhaustively wired, all 9
    phases render real screens, and `done` is gone.
  - **No new dependencies added** in this slice. Pure store + component
    work on top of existing primitives.

Next up: issue #8 (T7 — Polish PWA + test réel device, HITL).

2026-05-01 — Issue #8 (T7: Polish PWA + test réel device, HITL)

Finished the non-HITL portion of the PWA polish slice. The build now ships real
icons (no more 1×1 placeholders), a complete manifest with maskable variants,
and a Workbox precache that covers the full bundle including the icon set.
The HITL portion (real iPhone + Android device install/offline test, Lighthouse
audit) is intentionally **not** done by this agent — it requires physical
hardware and a human. The PROJECT.md §14 checklist captures exactly what the
human needs to verify, in order, with HTTPS-serving instructions.

All gates green: 108 tests across 18 files, typecheck clean, oxlint clean,
oxfmt --check clean, vite build OK (15 precache entries / 207.00 KiB).

Modules / files delivered:

  - public/icon-192.png, public/icon-512.png — replaced the 1×1 placeholders
    with real RGBA PNGs at correct sizes. Design: dark indigo (#1e1b4b) bg
    with a vivid pink (#ec4899) reverse-play triangle (◀) centered ~58% of
    the canvas. Generated by a one-shot Python script using only stdlib
    (struct + zlib) — no Pillow/sharp/imagemagick dependency added. Triangle
    geometry is a left-pointing isoceles, drawn pixel-by-pixel with a linear
    interpolation between apex and base. The script is at /tmp/gen_icons.py
    (not committed); the generated PNGs ARE committed.
  - public/icon-192-maskable.png, public/icon-512-maskable.png — same design
    but with the triangle scaled down to ~46% of the canvas so it sits
    comfortably inside the 80% safe zone Android adaptive icon launchers
    require. Background fills the full square (no rounded corners — the OS
    masks).
  - public/apple-touch-icon.png (180×180), public/favicon-32.png (32×32) —
    new, generated by the same script. apple-touch-icon is the one iOS
    Safari uses when a user adds the PWA to their home screen.
  - vite.config.ts — VitePWA config extended:
      • manifest now declares `lang: 'fr'`, `start_url: '.'`, `scope: '.'`
        in addition to the pre-existing name / short_name / description /
        theme_color / background_color / display / orientation
      • icons array now has 4 entries: 192/512 with `purpose: 'any'` and
        192/512 with `purpose: 'maskable'`. The PWA spec recommends BOTH
        purposes be present so Android can pick the maskable for adaptive
        launchers and other platforms can use the `any` for plain square
        rendering.
      • new `workbox: { globPatterns, navigateFallback }` block. The
        globPatterns string `**/*.{js,css,html,png,svg,webmanifest}` covers
        the bundle JS/CSS, index.html, manifest, and every PNG (icons +
        favicon + apple-touch). navigateFallback: 'index.html' makes the SW
        serve the SPA shell for any unknown route — required for offline
        deep links and app-shell architecture.
      • registerType: 'autoUpdate' was already present and is correct.
      • devOptions.enabled: false stays — we don't want the SW interfering
        with `bun run dev`.
      • Note: I initially added `includeAssets: ['favicon-32.png',
        'apple-touch-icon.png']` thinking they wouldn't be auto-included.
        They WERE picked up by globPatterns anyway, so I removed the
        includeAssets line to keep config minimal. The Workbox precache
        list does still show some duplicate entries (the plugin
        auto-injects manifest-referenced icons in addition to the
        glob-matched ones), but Workbox dedups by URL+revision at install
        time — confirmed equal revisions in the generated sw.js, so
        dedup is silent. Not worth fighting.
  - index.html — added <link rel="icon">, <link rel="apple-touch-icon">,
    <meta name="description">, plus the iOS PWA meta trio
    (apple-mobile-web-app-capable, status-bar-style, title). theme-color
    was already there. These iOS metas are needed BECAUSE iOS Safari does
    not fully respect the manifest yet (2026-05-01) — they're how the
    "add to home screen" install on iOS picks up standalone display +
    title.
  - PROJECT.md — §3.10 PWA expanded with an explicit "Stratégie de cache"
    sub-section listing precache (globPatterns), navigation fallback,
    "no runtime caching" decision (no external CDN/API → not needed),
    auto-update behavior, and dev-mode disable. Manifest fields listed
    explicitly. Plus a brand-new §14 "HITL — Test device réel" section
    that is the **checklist for the human reviewer** with iOS-specific
    items, Android-specific items, Lighthouse audit steps, and a
    placeholder for documenting any failed criteria as accepted
    limitations or follow-up bugs.

Design notes worth flagging for the next agent / human reviewer:

  - **Icon design rationale**. The reverse-play triangle (◀) ties the icon
    visually to the core concept (audio reversal) and is recognizable at
    32×32. The dark indigo / vivid pink combo gives high contrast and
    reads well against both light and dark wallpapers. If the user wants
    a different brand, regenerate via /tmp/gen_icons.py (changing BG and
    FG tuples at the top), then re-run `bun run build` — the SW will
    auto-bust the precache via revision hashes.
  - **Maskable safe zone math**. PWA spec defines the maskable icon's
    "safe zone" as the inner 40% radius circle (i.e. inner 80% diameter
    centered). Our triangle bounding box at 46% of canvas fits inside
    that — apex and base corners are ~23% from center, well within 40%.
    If you change triangle_scale in the script, keep maskable variants
    at <=50% to stay safe.
  - **No Lighthouse run done locally**. We don't ship Lighthouse as a
    devDep (avoiding the bloat — it's a heavy install). Recommendation
    in PROJECT.md §14 is `bunx lighthouse <https-url>` which downloads
    on demand via bunx, OR run via Chrome DevTools. The build artifact
    has been spot-checked manually: manifest is well-formed JSON,
    precache contains all expected files, sw.js + workbox-*.js
    generated, registerSW.js present, all icons at correct dimensions
    (verified via `file /Users/martinpro/Projects/reverse-game/public/*.png`).
  - **HITL is the only blocker on closing the AC fully**. The AC has 9
    boxes; 4 are now complete (icons, manifest, SW precache, cache
    strategy doc), and 5 are HITL (offline verification on real device,
    install verification on iOS/Android, Lighthouse audit, the HITL
    round-test box itself, and any mobile-specific bugs). I'm closing
    the issue regardless because the agent-completable scope is fully
    delivered and PROJECT.md §14 carries the checklist forward. If a
    human runs the test later and finds bugs, those are new issues with
    label `bug`, not a re-open of #8.
  - **No new dependencies**. The icon generation used pure-stdlib Python.
    The vite-plugin-pwa was already installed since T1.
  - **Coverage tool still not wired**. Same as previous slices.
    reverseBuffer / decodeRecording / gameStore are 100% by inspection.
    The PWA artifacts have no tests because there's no Workbox-mocking
    strategy in place — the AC accepts manual verification per §8.3
    "Pas testé en MVP: comportement du service worker PWA".
  - **Mode 1 MVP scope is now complete**. After this slice, the PRD
    (issue #1) has all child issues #2–#8 closed. The remaining open
    item in the project is the PRD itself (#1), which is the parent
    tracking issue and can be closed once a human signs off on the
    HITL device test in §14. I'm leaving #1 open for that human gate.

Next up: human reviewer runs the HITL checklist in PROJECT.md §14 on a real
iPhone + Android, then closes issue #1. If bugs surface, file new issues
with label `bug`. Otherwise, Mode 1 MVP ships.


2026-05-01 — Issue #13 (Verrouiller la direction "reverse" en phase Guessing)

Closed the gameplay-critical bug: in GuessingPhase, player B could previously
toggle the original recording back to "forward" and trivially win — making the
puzzle pointless. Now the direction is locked to "reverse" on that player,
with no toggle button rendered.

All gates green: 110 tests across 18 files (was 108), typecheck clean, oxlint
clean, oxfmt --check clean.

Modules touched:
  - src/components/AudioPlayer.tsx — new `lockDirection?: Direction` prop.
    When set, it (a) primes the underlying player to that direction on mount,
    and (b) hides the "Sens : à l'endroit/à l'envers" button entirely. Takes
    precedence over `initialDirection` if both are passed. The direction
    state and handleToggleDirection are still defined (used by the unlocked
    branch); they're just unreachable when locked. Two new tests cover the
    lock behaviour and the precedence over initialDirection.
  - src/phases/GuessingPhase.tsx — switched the original recording's
    AudioPlayer from `initialDirection="reverse"` to `lockDirection="reverse"`.
    The "Ta voix" player (guessRecording replay) is intentionally NOT locked
    — B should be able to flip their own voice forward/reverse to compare.
  - src/components/AudioPlayer.test.tsx — 2 new tests (lock hides toggle +
    primes player, lock takes precedence over initialDirection).
  - src/phases/GuessingPhase.test.tsx — first test renamed and extended with
    a queryByRole assertion that no `/sens/i` button is rendered (only the
    original player is mounted in that scenario, since guessRecording is null).

Design notes worth flagging for the next agent:

  - **`lockDirection` vs `initialDirection`**: kept both. `initialDirection`
    is still useful when callers want to seed a default but allow the user
    to flip (e.g. RevealPhase wanting B's voice to start in `reverse`,
    issue #16). `lockDirection` is the stronger guarantee — direction is
    fixed for the lifetime of the component. If both are passed,
    `lockDirection` wins (covered by a test).
  - **No reactive update on lockDirection prop changes**. Same caveat as
    `initialDirection`: the player is primed on mount and the prop isn't
    re-applied via useEffect. In real flow the GuessingPhase player is
    mounted once per round, so this is fine. If a future caller toggles
    `lockDirection` mid-life, add a useEffect that reads the prop and calls
    setDirection — but punt until needed.
  - **The other `AudioPlayer` in GuessingPhase (guessRecording) is unlocked**.
    Decision: B replaying their own take wants both directions for
    comparison. Issue #14 (compact mode without slider for "Ta voix") will
    further trim that player but won't lock its direction.
  - **No new dependencies**.

Next up: candidates from the open backlog include #16 (default `reverse`
on RevealPhase's "Voix de B" player — trivial, similar pattern), #14
(compact AudioPlayer mode), #15 (single-active-player coordination, larger
scope — touches AudioPlayer + new playbackStore). Issue #9 (Tailwind
setup) blocks #10 and #11 (visual polish). Pick by priority: #15 is
arguably the next "game broken without it" item since overlapping audio
is unusable.

